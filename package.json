{
  "name": "app",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm:start --prefix ./services\" \"npm:start --prefix ./client\"",
    "install": "cd services && npm run install && cd ../client && npm i"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
